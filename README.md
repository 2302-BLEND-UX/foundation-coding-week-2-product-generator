## Product generator with validation
# Generates a product based on a name and imageURL

- Validation uses if statements to check if both fields are empty
- Resets the inputs after a successful product submission